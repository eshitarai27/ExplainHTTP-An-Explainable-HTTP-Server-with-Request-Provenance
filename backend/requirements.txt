# ExplainHTTP's HTTP server itself uses only the Python standard library.
# This file exists for deployment tooling (Docker, Railway, Render) that
# expects one; it intentionally has no runtime dependencies.
