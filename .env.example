# Used by docker-compose.yml to bake the API URL into the frontend build.
# The browser must be able to reach this URL directly, so it should be the
# publicly reachable address of the backend service, not an internal
# Docker network name.
VITE_API_BASE_URL=http://localhost:8080
